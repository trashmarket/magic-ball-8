import './styles/page1.css';

const wepCheckPack = () => {
  const alpha = 'dog-wolf';

  const Nik = 'Nik';

  document.body.textContent(alpha + Nik)

  const f = new Promise()
f.then((res) => res());
};

wepCheckPack();
